# NLP Project (2025)

## Setup
```bash
# create env (conda)
conda create --name nlp python=3.11 -y
conda install -y jupyter jupyterlab ipykernel
conda install -c conda-forge numpy scikit-learn matplotlib transformers pandas tqdm pytorch torchvision torchaudio spacy -y
